# ── Publish Caddy's HTTP port to the host ────────────────────────────────────
# Included when proxy=caddy AND no tunnel fronts it. With a tunnel, cloudflared
# reaches caddy:80 over the compose network, so this overlay is omitted.
services:
  caddy:
    ports:
      - "${PROXY_HTTP_PORT:-80}:80"
